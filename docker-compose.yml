version: '3.8'

services:
  image-analyzer:
    build: .
    container_name: image-analyzer-app
    ports:
      - "8081:8081"
    volumes:
      - ./data:/app/data
    environment:
      - PYTHONPATH=/app/src
    restart: unless-stopped